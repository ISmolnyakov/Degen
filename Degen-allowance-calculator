#can be run in any python interpreter like replit or ide like Pycharm

locked_amount = 396_448_914 #current locked amount
my_amount = 100_000 # your locked amount

my_precent = round((my_amount / locked_amount) * 100, 2)
my_allovance = round((my_precent * 4_500_000) / 100, 2)
result = round(my_allovance * 30, 2)

print(f"My Percent: {my_precent}")
print(f"My Allovance: {my_allovance}")
print(f"Result: {result}")
